# EF Version Explainer — dependencies (deliberately minimal)
pandas>=2.0
openpyxl>=3.1          # read/write DEFRA .xlsx workbooks
pdfplumber>=0.11       # read the DEFRA "major changes" PDF
rapidfuzz>=3.0         # fuzzy string matching for BOM -> factor matching
streamlit>=1.30        # the single-page UI
anthropic>=0.40        # the Claude explanation layer

# dev / test
pytest>=8.0
